/**
 * Built-ins advertised by the ZCode App protocol.
 *
 * This catalog is intentionally separate from the broader CLI TUI help registry.
 */
export const APP_PROTOCOL_VISIBLE_BUILTIN_SLASH_COMMAND_NAMES = [
  "goal",
  "compact",
  "init",
  "skill",
] as const;
