# Milestone #2 集成候选复验：`2b06484c5f8c88f31c6b919fd7c0ff7bf74d7255`

本目录只记录上述完整**产品代码提交**在 2026-09-26 macOS Apple Silicon 的本机复验；证据提交可晚于产品提交。所用 Electron 41.0.3、Node 24.14.0、pnpm 10.33.2，ZCode CLI `zcode-cli/0.16.9@872ad960de7ec172591f7e1952f7849229f94521`，Adapter `m1.2`。隔离 `HOME=/tmp/anyagent-opencode-go.aBEA0z/home`，隔离项目仅为同路径的 `m2-final-project`、`m2-question-project`，已有 OpenCode Go 配置继续可用；证据没有复制凭据。M1 App 由 `launch-m2-final-m1.mjs` 启动，M0 flag-off App 由 `launch-m2-final-m0.mjs` 启动，均从本提交的工作树构建。正式真实路径是原 Root／Composer → Host／Runtime → ZCode Adapter／Service → 固定真实 CLI → 已配置模型。下述结构化核对见 [crosscheck.json](crosscheck.json)，截图由 CUA 在对应隔离 App 内直接取得。

## M1 真实 CLI 与产品边界

1. 冷启动后，原侧栏 Task `task_be2ae963-20c2-4f07-9c0b-b16dee2bccb5` 先显示历史可读、输入禁用和“恢复原会话”；显式点击恢复后才可提交。同一产品 Participant `participant_bb034b3d-d6a4-4843-86f4-018413a97ac2`／Session `session_1cd1d6fc-675c-44f6-bd1a-4b9adf275ed3` 对应原生 `sess_9d853192-d8fb-418f-af3b-fa6cb6dc0916`。A→B→A 导航后新轮 `M2_FINAL_2B0_R5_926` 的 Input `input_f774e43f-4b35-435a-a1c6-4cbb58b06986` 与原生 `queue_input_f774…` 均留在原身份，完成且没有新建原生 Session。模型未照提示输出预期标记，而重复了旧答复 `SECOND_MODEL_OK_925`；只以真实派发、归属和完成证明恢复，**不以其文本证明指令遵循**。
2. 恢复后的原 Composer 输入 `/`，[命令候选](slash-after-cold-restore.jpg)显示由当前固定 CLI 提供的 `/goal`、`/compact`、`/init`、`/skill`、`/plan`；正式 UI 发送 `/goal` 后原生回执“当前 Session 尚未设置目标”。该命令目录经 Host 当前 Task／Session／授权资格读取，不是旧 `zcodeSessionStore` 预填；组件与 Host 测试覆盖异步切 Task、刷新和暂不可用。另一独立 Task `task_ca5aeca7-3407-49a6-9ef5-ec9930924941` 也经显式恢复看到同一固定 CLI 的当前命令候选，没有把 Task A 的草稿或状态当作 B。`/goal` 没有产品 Input／Execution，符合原生命令语义。
3. 同一 Task A 的真实 Write 审批：[允许前](approval-allow-pending.jpg)出现原审批卡，选“仅允许这一次”后[结果](approval-allow-result.jpg)和隔离文件 `m2-final-2b0-allow-926.txt` 对应，内容为 `ALLOW_2B0_926`。另一轮的 Write [拒绝前](approval-deny-pending.jpg)被拒绝；模型自行再次提出 Write，第二个请求也被拒绝，[最终 UI](approval-deny-result.jpg)和两条产品 rejected 审批记录相符，`m2-final-2b0-deny-926.txt` 不存在。允许轮中模型文字误称“自动批准”，与实际 UI／审批记录矛盾，不用作审批事实。重复拒绝证明两次工具都未写文件，不表示模型永不再次提出请求。
4. Task A 在原 Composer 发送 `/compact`；产品只建 `compact-operation=compact_826e9834-f565-490a-b5aa-142c64aa3dce`，原生 ACK 后约 195 秒转为 completed，没有伪造 Input／Execution。[完成后的会话](compact-completed-session.jpg)仍可用，新轮 `M2_FINAL_2B0_AFTER_COMPACT_926` 在同一原生 Session 回答 `AFTER_COMPACT_2B0_926`。这次耗时属于实际模型／CLI 运行观测，不推断所有环境中的 compact 延迟。
5. 独立 Task C 在原 Composer 同时加入本地 `README.md` 附件和 `README.md` 文件引用，发出 `M2_FINAL_2B0_CONTEXT_926`。[回复窗口](attachment-mention-result.jpg)显示隔离项目的 Workspace marker。产品 Input 同时存文本 `[README.md](./README.md)` 与一个 34 字节附件，原生 `session_input` 的 `attachmentRefs` 指向同一隔离文件，产品 Task／Session 为 `task_ca5…`／`session_f600…`，原生为 `sess_8ac3…`，与 Task A 分开。模型对项目文件清单的额外叙述包含过时内容，不用来证明文件状态；文件状态以实际隔离目录和结构化记录为准。
6. 同一 Task C 的 `M2_2B0_QUEUE_A_926` 在[完成前流式生成](streaming-with-queued-b.jpg)时，原 Composer 将 `M2_2B0_QUEUE_B_ORIGINAL_926` 排入等待队列。用户从队列取回该草稿并改成 `M2_2B0_QUEUE_B_REVISED_926` 后[重新排队](queue-edited-before-promotion.jpg)。A 完成后 B 才[提升并执行](queue-revised-result.jpg)。产品原 B `input_e9bfc649…` 为 cancelled，原生没有该文本的 `session_input`；修订 B `input_e3a4ec68…` 在 Task C 原生 `sess_8ac3…` 中被提升。A 在结束前已可见增量内容，完成后没有重复消息。B 模型没有按提示输出预期标记，本场景只用来证明队列顺序、编辑和归属。
7. Task C 以 `M2_2B0_EDIT_SOURCE_926` 发送本地 `m2-final-allow-925.txt` 附件，随后从正式历史操作编辑为 `M2_2B0_EDIT_KEEP_926`，保留同一附件，再编辑为 `M2_2B0_EDIT_REPLACE_926`，移除旧附件并加入 `README.md`。[最终历史与附件](attachment-edit-source-keep-replace.jpg)以及产品三条 Input 的 `revisionOf`、附件元数据和原生 `session_input.payload.attachments` 一致；两次新版本分别指向紧邻来源 Input，原记录仍在。此处验证可编辑的最新已完成历史轮及其来源链，不把结果扩大为任意更早轮次均可编辑。
8. Task C 的 `M2_FINAL_2B0_QUESTION_926` 在真实 CLI 中触发原生 `AskUserQuestion`，正式原组件显示[结构化 A/B 选项](question-choice-pending.jpg)。从 UI 选择 B 后，产品 `user-input_a806717c…` 状态 `forwarded`、原请求 `perm_8fb3bdad…`、答案 `B` 及原 Execution `execution_afed8f3d…` 绑定一致；原生 `part` 的 `AskUserQuestion` 工具结果写明答复 B，同一 Input／Execution completed，[完成窗口](question-choice-completed.jpg)可见。模型最终没有按提示输出精确 `QUESTION_B_2B0_926`，因此本次证明原生提问、选择、转发和继续执行，**不以最终文字证明精确指令遵循**。产品／原生记录见 `crosscheck.json` 的 `question`。
9. 原 `⌘N` 快捷键打开新任务 Composer，未选择 Harness 时按原路径创建 M0 原生 Task；在新任务的原模型选择器显式选择 `Harness · zcode` 后，键盘提交 `M2_FINAL_2B0_SHORTCUT_HARNESS_926` 创建新的产品 Task `task_31653add…`／Session `session_ae9721a3…`，原生 `sess_37e4b711…`，得到[正式回复](shortcut-new-harness-task.jpg) `SHORTCUT_HARNESS_OK_926`；Input／原生 queue_input 见 `crosscheck.json`。这证明快捷键入口在两种显式模式下可用，不代表侧栏菜单的其它动作均已恢复。当前 M1 Task 侧栏[“更多”菜单](sidebar-menu-current.jpg)的置顶、重命名、归档、未读明确禁用并写“暂不支持”，其是否属于 #26 Required parity 仍需范围判定，不能记为已通过。
10. 上述新 ZCode Harness Task 第二轮 `M2_FINAL_2B0_SHORTCUT_HARNESS_R2_926` 仍在同一产品／原生 Session 完成。原模型选择器的 `Harness · zcode` 子菜单允许选择同 OpenCode Go (Anthropic) Provider 的 `deepseek-v4.1-flash`，第三轮 `M2_FINAL_2B0_MODEL_DEEPSEEK_926` 得到[真实回复](same-provider-model-deepseek.jpg) `MODEL_DEEPSEEK_OK_926`。产品 Input `input_2df75661…` 提交配置与原生 `message.modelSelection` 均为 `opencode-go-messages/deepseek-v4.1-flash`、reasoning `max`，原生 Session 未迁移。根层[旧 Provider 菜单](legacy-provider-group-locked.jpg)在已有 Harness Session 内锁定并提示不能切换，**同 Provider 换模入口是 Harness 子菜单**；该早期截图只证明锁定的根层，不是同 Provider 功能缺陷。其它 Provider 的 Harness 模型项灰显，未产生新 Input 或原生派发。
11. 同一 Task 下一轮从原模式选择器选“计划模式”，正式窗口[显示模式和真实回复](plan-mode-real-cli.jpg) `PLAN_MODE_OK_926`。产品 Input `input_7e65b4c4…` 的提交配置为 `mode=build, planEnabled=true`、deepseek／`max`，原生 user message 的 `metadata.inputIntent` 同样记录 `mode=build, planEnabled=true`；这是固定 CLI 的计划模式映射，不能误读为没有切换。本轮无工具或文件副作用。
12. 回到“变更前确认”，原 Composer 的 slash picker 选择 `/init` 并提交。固定真实 CLI 在隔离项目中读 README／目录，经过原权限组件一次允许只读命令、一次允许 Write，原生 Input `queue_input_162f609a…` 与产品 Input `input_162f609a…` 属同一 Task／Session 并完成。[正式文件摘要](init-command-file-summary.jpg)显示 `AGENTS.md +42`，实际隔离文件 2001 字节、SHA-256 见 `crosscheck.json`。这同时证明目录候选可选、命令派发、工具审批及文件摘要；只涉及隔离测试项目，不推断其它项目结构。`/init` 是一条真实 Input／Execution，与 `/goal`、`/compact` 的原生操作记录形态不同，不人为统一。

## M1 Fake 原产品路径

在同一候选的正式原 Root／侧栏／Composer 中新建 Fake Task A `task_8eec2cfa-95c9-4d0b-bc18-16a826c4ef8a` 与独立 Fake Task B `task_22f35830-4603-4043-afd6-de2cc64f2a94`。A 连续提交两轮、切到 B 提交一轮、再切回 A 提交第三轮，[A 的窗口](fake-a-after-b.jpg)只显示 A 的输入／回复／工具投影；产品库四条 Input 均完成，A 与 B 分属不同 Session，见 `crosscheck.json` 的 `fakeInputs`。重启后先前 Fake 历史可读，但内存 Fake Adapter 明确回报不能接回旧原生 Session；该场景没有将 Fake 冷恢复冒充真实 ZCode 冷恢复。

## 运行中观测丢失、Stop、对账

在 `m2-question-project` 的原 Task `task_f9b8cf36-ac2b-4cc2-9c94-830bb619c0a5` 显式冷恢复。一次性开发态 Adapter 观测中断只绑定该隔离项目、原生 `sess_5fa1f43f-9605-4083-aa51-f295516c7409` 与目标 Input，不断 Host–CLI stdio、不影响其他 Task。两次尝试均经正式 UI 的 Write 审批后，原生工具实际写文件，Adapter 在该原生工具结果后停止观察；产品 Input／Execution 进入 unknown，不重发输入。[unknown 与 Stop 入口](unknown-running-stop-entry.jpg)明确保留对账和请求中断。

第一次 Input `input_98e4b4ae-f353-43dc-afe6-1c75e5bf1833` 在点击 Stop 前原生已经成功终结；Host 拒绝过时 Stop，提示 `guard.stopTargetChanged`，随后对账同一执行为 completed。这是正确的目标变化负例，不算成功停止。第二次 Input `input_46728b4b-e3ee-40b2-be0a-3b161f769675` 在原生仍运行时点击“请求中断”；[送达 UI](stop-delivered-not-confirmed.jpg)写明 ACK 不代表已停止。原生随后记录同一 Input `turn_complete/cancelled`，产品保持 unknown 直到显式点击“对账原执行”，才[显示已停止](stop-reconciled.jpg)，并警示观测丢失期间的工具／文件事件未重建。隔离文件 `m2-final-2b0-stop2-926.txt` 实际内容为 `STOP2_SIDE_EFFECT_2B0_926`，产品和原生各只有一条对应 Input。停止后的新轮 `M2_FINAL_2B0_AFTER_STOP_926` 在同一产品／原生 Session 完成并[回复标记](after-stop-same-session.jpg)。本场景证明 Adapter 事件观察中断后的 unknown、safe Stop 与原执行对账；**不声称已验证物理 stdio 断开**。

## M0 flag-off 独立对照

关闭 M1 开关后，原 Root、项目侧栏和 Composer 仍可用。隔离原生历史 Session 的[原命令菜单](m0-slash-existing-session.jpg)列出 `/goal`、`/compact` 等；同一原对话发送 `M2_FINAL_2B0_M0_SMOKE_926` 后得到[真实回复](m0-flag-off-smoke.jpg) `M0_FLAG_OFF_OK_2B0_926`。从该对话进入[自动化](m0-automation-navigation.jpg)和[模型设置](m0-model-settings.jpg)，已配置的 OpenCode Go 两个供应商显示就绪；[返回原任务](m0-return-same-task.jpg)仍保留该轮。此为当前提交的 flag-off 回归，历史 M0 Verified Baseline 的验收范围仍以 Milestone #1 当时证据为准。

## 能力边界与沿用证据

本候选的真实 UI 已覆盖当前 Task 的 slash 目录、`/goal`、`/compact` 和 `/init`、真实结构化 question 显式答复、build／`qwen3.8-flash`／`xhigh`、同 Provider 换到 deepseek／`max` 及计划模式的真实提交配置；`crosscheck.json` 逐 Input 记录实际提交值。更早候选的固定 CLI 问题空答见 [final-a314e82](../final-a314e82/README.md)，只证明其原场景。question 过期／迟到／重复和资格失效由本候选自动化覆盖，真实桌面场景只证明上述显式答复。

## 检查与证据边界

在本产品提交运行：`pnpm run verify:anyagent-core`（Contract 12/12、Runtime 81/81）、`pnpm exec tsx --test packages/services/test/anyagentZcodeAdapter.test.ts packages/services/test/anyAgentService.test.ts`（88/88）、`pnpm --dir packages/ui test`（34/34）、原生 session mapper（3/3）、隔离 stdio failpoint（4/4）、`pnpm typecheck`、`pnpm lint`（65 条既有 warning、0 error）、`pnpm architecture:check -- --changed`（0 violation）、`pnpm build:bootstrap`、ADR index check、`git diff --check`，均本机通过。无适用远端 CI；以上不是 CI 结果。UI 测试有既有 React `act(...)` 警告。当前候选的 Fake、队列、编辑、question、换模与 M0 flag-off 已按上文回填；侧栏菜单仍有待修的 Required parity 缺口，不能宣称完整 Milestone closure。最终独立 reviewer 尚未执行。
