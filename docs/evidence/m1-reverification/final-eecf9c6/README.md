# 产品代码候选 `eecf9c6` 的桌面复验

被测产品提交：`eecf9c686ec820e2a5a3c1c38f74eb4866ac5249`。本目录截图均来自该提交构建的隔离 macOS App；此前提交的截图只保留为历史单项证据。[唯一收口矩阵](../README.md#m0-单参与者能力收口矩阵)记录各能力的最终 A/B/C/D 状态，本文件不是第二份验收台账。

## 环境与执行路径

- macOS Darwin arm64；Node 24.14.0、pnpm 10.33.2、Electron 41.0.3；仓内真实 `zcode-cli 0.16.9`，ZCode Adapter `m1.2`。
- 复用隔离 App 已有的 OpenCode Go (Responses) 配置与 `grok-4.6`、`gpt-5.6-luna`；凭据没有复制到仓库、截图或记录。数据、原生 SQLite、测试项目及启动器均在 `/tmp/anyagent-opencode-go.aBEA0z/`，正式用户数据未被修改。
- `pnpm build:bootstrap` 后，经隔离启动器执行 `pnpm dev:desktop:prod`。M1 用 `ANYAGENT_M1_WORKBENCH=1`，M0 用 `0`，两者均运行上述相同产品代码提交。M1 路径为原 Root / Composer → Host RPC → Runtime → ZCode Adapter → `IZCodeAgentService` → 真实 CLI → OpenCode Go；M0 开关关闭后走原 Provider 对话路径。
- 同一场景以窗口截图、只读产品 SQLite、原生 Session / `session_input`、隔离文件状态及 App 日志互相核对。以下 ID 均来自该隔离数据集。原始 App 日志在 `/tmp/anyagent-opencode-go.aBEA0z/app-pr25-eecf9c6.log` 与 `app-m0-eecf9c6.log`；未提交可能含本地路径和配置的完整日志。

## 最终候选上的真实 CLI

原侧栏旧 Task `task_d72ea5b7-21fa-4239-af6c-0fed483ff506`、Participant `participant_7fc03a17-7361-4cc5-8933-59d1da37f74e`、产品 Session `session_7bf85d92-91f2-4201-8517-ed560eef0080` 在 App 重启后仍可读。点击“恢复原会话”后关联原生 Session `sess_3ff5b616-237f-41c5-a7aa-3ad68e0e62c3`，没有新建产品 Task / Session 冒充恢复；后续轮次留在这些身份下。旧候选上失败的提问记录仍保留，不被本次成功记录改写。该事实仅证明当前有效配置下的正常恢复；配置变化、授权失效和业务终态另列 D。

| 场景与操作                 | 同场景记录、结果与截图                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 结构化提问                 | 正式 UI 触发原生 `ElicitationDialog` 单选 A/B，选择 B 后原生执行继续并返回 B。产品 UserInput `user-input_333036d6-69ab-4b62-8465-89f4442aebfa`、Input `input_7307adb7-1179-453a-85cf-2c1c2eda3ad0`、Execution `execution_26b9fd63-5717-4edb-8e1d-2cba1d16c4af` 属上述 Task / Session。[提问中](native-question-pending.png) · [答复后](native-question-completed.png)                                                                                                                                                                                |
| 同 Provider 换模型与多轮   | 原选择器从 `grok-4.6/xhigh` 切至 `gpt-5.6-luna/max`，标记 `PR25_MODEL_FINAL_EECF9C6` 返回 `MODEL_LUNA_EECF9C6`；下一轮返回 `QUEUE_EECF9C6`。产品 `submissionConfig.modelSelection` 与原生 assistant 消息 `providerId/modelId` 同为 `opencode-go-responses/gpt-5.6-luna`；早轮原生消息为 `grok-4.6`，产品与原生 Session 不变。[逐轮模型](native-model-switch.png)。模式与推理档位的逐轮生效没有完整互证。                                                                                                                                             |
| 原生新建命令及未知命令     | 原 Composer 选择并执行 `/new` 后创建独立产品 Task `task_149b4e67-2aee-41c0-aed4-56f0f2b0bf54`、Participant `participant_8f96ca1a-35fb-4078-b3a5-15c4911a82ba`、Session `session_1cb6a628-6a83-4bb1-a09e-fd0678a98bca`，零业务 Input。[新任务](native-slash-new.png)。随后 `/goal status` 显示不可映射原因并保留草稿，没有派发伪业务 Input；这不等于全部 CLI 命令已接通。[未映射](native-slash-unmapped.png)                                                                                                                                          |
| 中断                       | 长回答 `PR25_STOP_FINAL_EECF9C6` 的增量先在正式流可见，点击“请求中断”后先显示请求中，待原生证据到达才显示“已停止”。产品 StopRequest `stop_34dc2cbc-7dd5-4ed9-9fd4-c9bd34d28b5a` 的送达证据 `726c847b-17f5-4a40-9b92-170214b9ee33` 与停止证据 `5f47e217-a197-4544-a288-3a22303825ec` 不同；Input `input_7d8db472-fcc0-48c4-b244-5bc1aafe64ce`、Execution `execution_f142ec57-030e-4f15-9355-4bcfdb63459a` 最终 stopped。[流式中](native-stream-before-stop.png) · [原生确认后](native-stop-confirmed.png)。CLI 重启后的旧执行停止尚无安全机制及实测。 |
| Write 允许审批与文件副作用 | 原 `PermissionDialog` 仅点击一次“允许”；产品 Approval `approval_0dd6171d-435e-4c8c-be23-f77367e5290f`、Input `input_82085a56-76a0-45ed-b827-e393175d8037`、Execution `execution_f0667e36-3f42-43d0-8efe-818e9d2c95d7`，完成后隔离项目 `pr25-final-eecf9c6.txt` 内容为 `APPROVED_EECF9C6`，正式回答 `WRITE_EECF9C6`。[审批中](native-approval-pending.png) · [完成后](native-approval-completed.png)。最终候选上拒绝、迟到和失效答复尚未走完原生回归。                                                                                                |
| 文件撤销                   | 同一 Write 的原文件摘要显示预览安全 1 / 不安全 0；执行后隔离文件不存在，产品 FileRewindOperation `file-rewind_ef068636-f93c-4970-a242-2c87e9e4dba2` 为 applied，UI 显示“已撤销”。[预览](native-rewind-preview.png) · [执行后](native-rewind-applied.png)。未知结果下防重复仅有较低层测试。                                                                                                                                                                                                                                                           |
| `/compact`                 | 原 Composer 命令生成原生维护操作 `compact_f0dff1ec-878f-46be-a892-955384ec31fa`，最终 completed，没有伪造 Input / Execution；之后同 Session 的 `PR25_AFTER_COMPACT_EECF9C6` 返回 `AFTER_COMPACT_EECF9C6`，Input `input_a9b8aa01-533f-4355-82e8-66cee60b1f90` / Execution `execution_76cbcf1b-cbba-4dce-9468-8e75c0787698` completed。[压缩后](native-compact-completed.png) · [续聊](native-after-compact.png)。带自定义 instructions 的参数路径未接通。                                                                                             |
| 真实 CLI 队列              | 源输入 `PR25_QUEUE_SOURCE_EECF9C6` 流式中，通过“加入队列”提交 `PR25_QUEUE_FOLLOW_EECF9C6`；源终结后才返回 `QUEUED_AFTER_SOURCE_EECF9C6`。源 Input `input_4ee11ed9-0936-486f-a434-39024da71f9e` 为 startNow，后续 Input `input_6b86c6ee-1f05-4f0d-a601-b13dde65f0b6` 为 queue，原生 `session_input` admitted seq 14 / 15、promoted seq 13 / 14，各推进一次。[排队中](native-stream-queue-pending.png) · [完成后](native-queue-completed.png)。取消、编辑、立即发送及未知执行下恢复仍待验证。                                                          |

## Fake 与 M0 对照

最终候选原 UI 中，Fake A `task_c60a7d14-b874-4e0e-a935-db56fdc543f2` / `session_7cb76893-793e-4b7a-9ec7-dfc0749da668` 完成 R1、R2、R3；独立 Fake B `task_92d787d1-cd29-4082-a306-ce46e431bbd4` / `session_30488348-2081-476b-87c1-c3ccba466894` 完成 R1。原 toolbar 和 `Cmd+N` 分别创建 Task；B→A 后 A 的第三轮仍属 A，去自动化页再返回仍能阅读 A 历史。[A→B→A](fake-a-b-a.png) · [返回后](fake-return-from-automation.png)。Fake fixture 的回答轮数计数器跨 Task 累加，不代表产品轮次混线；SQLite 的 Task / Session / Input 归属正确。

相同产品提交关闭 M1 flag 后，启动 URL 无 `anyAgentServiceEnabled`，原选择器只显示 Provider；原 Composer 提交 `M0_EECF9C6`，OpenCode Go 返回 `M0_NATIVE_EECF9C6`，原生 Session `sess_a468c856-aa27-4c89-82cf-ed08fc23de08` 的 `session_input` seq 0 admitted / promoted。自动化和模型设置页可达，设置中无 Harness 入口。[M0 原对话](m0-native.png) · [Provider 选择器](m0-provider-selector.png) · [模型设置](m0-settings.png)。M0 固定行为另以 [ZC-04](../../../architecture/zcode-transition.md#zc-04m0-整体-bootstrap-过渡基线) 与原 Root 入口对照；此处只证明列出的最终候选关闭开关回归。

## 检查、层级和边界

同一产品提交运行：Contract 9/9、Runtime 47/47、Service / Adapter / CLI / attachment 66/66、UI 挂载与 DOM 28/28；`pnpm typecheck`、`pnpm lint`（0 error、65 warning）、`pnpm architecture:check -- --changed`（0 violation）、`pnpm build:bootstrap`、ADR 索引检查、修改文件格式检查及 `git diff --check` 通过。UI 测试仍有 React `act` 警告。仓库没有适用 `.github` CI；以上为本机结果。完整命令和历史增量范围见[主证据](../README.md#最终代码候选的检查)。

真实 CLI 结果只覆盖上表路径；Fake、协议模拟、组件测试分别验证自身层级，不能替代真实 CLI。网页元素和共享上下文导入已有新代码与较低层测试，但最终桌面真实 CLI 尚未验证。附件 / mention / 编辑 / 反馈 / 分叉 / Retry、权限模式、失效授权、业务终态、连接中断恢复、迟到审批以及新建入口全组合仍有 D 项；早期实测留在[原生能力增量记录](../native-parity-2026-09-24.md)并保留原提交归属。CLI 的执行 ID 为进程内编号，重启后可能复用，不能据旧 ID 安全地给旧执行发送 stop；这项限制尚未获维护者豁免，不算 B。
