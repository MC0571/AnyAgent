# `07bced3` 产品候选的本机复验记录

完整被测产品提交：`07bced37c5e82e1a9413b305dc2f0fa0c2358d85`，基线 `origin/main`：`644120db21f654edbf40b4008df92294772a08cc`。这是本候选的实际结果；[唯一收口矩阵](../README.md#m0-单参与者能力收口矩阵)决定最终分类。后续代码修复须另记候选并重跑受影响路径。

## 环境与检查

macOS Darwin arm64、Node 24.14.0、pnpm 10.33.2、Electron 41.0.3、固定真实 `zcode-cli` 0.16.9、仓内 ZCode Adapter `m1.2`。隔离 home、项目和 Electron 数据位于 `/tmp/anyagent-opencode-go.aBEA0z`，复用隔离 App 内已配置的 OpenCode Go (Responses)；本候选实测 `gpt-5.6-luna` 与 `qwen3.8-flash`，具体轮次见 JSON。未读取、提交或截图展示凭据。M1 使用仓库 `pnpm dev:desktop:prod`，`ANYAGENT_M1_WORKBENCH=1`；本轮分享导入连向仅监听本机的测试 HTTP fixture `127.0.0.1:43127`，fixture 源码版本 `pr25-fixture-028efd12886f433386eb`。原始 App 日志只留隔离目录 `app-final-07bced3.log` 与 `app-final-07bced3-restart.log`，仓库仅保存选取字段。

| 本机命令                                                                                                                                                                                                                              | 结果                                                |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| `pnpm --dir packages/anyagent-engine test`                                                                                                                                                                                            | Contract 9/9                                        |
| `pnpm --dir packages/anyagent-runtime test`                                                                                                                                                                                           | Runtime 53/53                                       |
| `node --import tsx --test packages/services/test/anyAgentService.test.ts packages/services/test/anyagentZcodeAdapter.test.ts packages/services/test/promptAttachmentTransferService.test.ts`                                          | Service / Adapter / attachment 75/75                |
| `node --import tsx --test apps/zcode-cli/packages/bootstrap/test/session-mapper.test.ts apps/zcode-cli/packages/bootstrap/test/native-turn-terminal-provenance.test.ts apps/zcode-cli/packages/bootstrap/test/slash-commands.test.ts` | 原生映射 / terminal / slash 4/4                     |
| `NODE_OPTIONS=--max-old-space-size=1024 pnpm --dir packages/ui test`                                                                                                                                                                  | 实际挂载与 DOM 30/30；仍有 React `act` 告警         |
| `pnpm typecheck`；`pnpm lint`；`pnpm architecture:check -- --changed`；`pnpm build:bootstrap`；`bash docs/decisions/generate-index.sh --check`；修改文件格式；`git diff --check`                                                      | 通过；lint 0 error、约 65 warning；架构 0 violation |

检查日志在本机 `/tmp/pr25-final-07bced3-*.log`。仓库无适用 `.github` CI，PR 检查列表为空；上述是本机检查。

## 真实 App 操作与结果

操作均从正式 Root、原侧栏和原 Composer 发起，主路径为 UI → Host / Runtime → ZCode Adapter → 既有服务 → 真实 CLI → 已配置真实模型。产品和原生 SQLite 仅作只读对账；截图与选取字段均来自同一隔离运行。

| 场景                                        | 实际结果与证据                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 新建 Harness Task，原 Composer 首轮真实模型 | `gpt-5.6-luna` 返回 `BASE_READY_925`。随后从原“添加上下文”导入分享 fixture，创建新产品 Task / Participant / Session、原生 Session 和授权，不复用首轮 Task；[产品与原生身份](shared-context-native-crosscheck.json)。fixture 导入经 `verify-import.mjs` 只读检查通过。                                                                                                                                                                                                                |
| 分享上下文导入后续聊                        | 原生 `qwen3.8-flash` 回答与导入 JSON 唯一 marker 完全一致。产品 Input `input_b79953e6-8061-4717-bcea-ca040e55e5d4` 与原生 user message 的 `inputIntent.sharedContextRefs` 指向同一 context；[窗口](shared-context-native-result.png)、[两层存储](shared-context-native-crosscheck.json)。这证明本机 fixture、Host、CLI 和真实模型路径，不证明生产分享服务可用。                                                                                                                      |
| 同 Task `/compact`                          | 原 Composer 命令创建 `compact_7353d488-0154-4495-816e-0bb58db0f084`，终态 completed，没有伪造业务 Input / Execution；随后原 Session 普通轮返回 `AFTER_COMPACT_OK_925`。[操作后窗口](shared-context-after-compact.png)、[续聊](shared-context-compact-resumed.png)、[存储](shared-compact-crosscheck.json)。重复 / busy 负例尚未完成。                                                                                                                                                |
| Queue send now 与中断                       | 长源轮运行时排 A 和 B，对 B 点“立即”；UI 先显示等待原生停止，再确认源轮 stopped，原生 B 先于 A 派发，两者完成。[等待](queue-send-now-pending.png)、[完成窗口](queue-send-now-completed.png)、[双库序号与归属](queue-send-now-crosscheck.json)。**发现阻塞**：B 的独立原生结果先写出已停止源轮要求的 120 句，再返回 `QUEUE_B_925`；A 仅返回 `QUEUE_A_925`。原生 history 含已停止但未答的源 user prompt，模型在 B 轮继续完成它。不能把此场景判 A，需修复原生历史上下文及复测。         |
| 冷启动旧 Task                               | 同一分享 Task 在多轮、compact 和队列后退出 App，再以相同隔离数据启动。原侧栏点击旧 Task 时历史可读，但 Session 未恢复，输入禁用；显式点击“恢复原会话”后，原 Task / Participant / 产品 Session / 原生 Session 和授权保持同 ID，新增真实 CLI 轮返回 `COLD_RESTORED_925`。[恢复前](cold-restore-before.png)、[新轮结果](cold-restore-completed.png)、[产品与原生对账](cold-restore-crosscheck.json)。这只证明当前资格有效的正例；配置改变、失效授权、业务终态及跨 Task 负例仍在矩阵 D。 |
| 内置浏览器网页元素                          | 隔离本地 HTTP 页面 `127.0.0.1:43128` 可在原右侧浏览器打开，标记按钮可见。启动“选择网页元素加入聊天”后以桌面自动化直接点击按钮，拾取器退出但 Composer 未出现 chip；[失败画面](web-element-no-chip.png)。日志只有“选择已取消”，没有加入事件。独立排查指出 picker click handler 依赖此前的 `mousemove`；本次输入没有该事件。此候选不能判网页元素接入通过，后续需补 click 路径并复测原生输入。                                                                                           |

本候选还补了 Input 原生 ACK 丢失后的手动只读对账入口、stable command ID、迟到授权复核、已对账且缺工具轨迹轮的重试阻断，以及 slash 命令选择器和 locked 模型解释；对应 Contract / Runtime / Service / Adapter / UI 测试见上表。协议模拟不能代替真实 CLI 断线重连、ACK 丢失和副作用路径的桌面证据。独立代码复审发现并促成了对账写入前授权再核验、已对账失败轮重试防副作用两项修复；复审未替代最终产品验收。

本候选仍有未覆盖的既定桌面路径，包括完整 M0 关开关对照、Fake 多 Task、最终候选的真实审批/工具和模型逐轮切换、实际断线对账、过期审批、失败重试、文件撤销未知结果、网页元素、全部 Task 入口和组合导航。较早候选证据在上级矩阵按原提交保留，不能移作本候选结论。
