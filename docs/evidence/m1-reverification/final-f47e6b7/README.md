# PR #25 产品代码 `f47e6b7` 增量复验

完整被测产品代码提交：`f47e6b7679f199d82d2f78fc8534aa417b110367`；基线 `origin/main`：`644120db21f654edbf40b4008df92294772a08cc`。此目录是当前候选已做场景的证据，最终分类仍只在[三对象收口视图](../README.md#milestone-2-收口视图三个独立对象)维护。未重测的旧候选路径不能由此目录补算通过。

## 环境、正式路径与检查

macOS Darwin arm64；Node 24.14.0、pnpm 10.33.2、Electron 41.0.3、固定真实 `zcode-cli` 0.16.9、仓内 ZCode Adapter `m1.2`。隔离 App home、Electron 用户数据和项目均在 `/tmp/anyagent-opencode-go.aBEA0z`。复用隔离 App 中现有 OpenCode Go (Messages) 配置，本候选真实模型为 `opencode-go-messages/qwen3.8-flash`，推理档位 `xhigh`。正式 M1 主路径为原 Root／Composer → AnyAgent Host／Runtime → ZCode Adapter → 既有 ZCode 服务 → 真实 CLI → 已配置模型服务。关闭 M1 后核对的 M0 对话使用原 Host／ZCode 路径。没有把凭据写入仓库。

| 本机命令                                                                                                                                                                                                                                                                                                                                                                       | 实际结果                                           |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------- |
| `pnpm --dir packages/anyagent-engine test`                                                                                                                                                                                                                                                                                                                                     | Contract 9/9                                       |
| `pnpm --dir packages/anyagent-runtime test`                                                                                                                                                                                                                                                                                                                                    | Runtime 57/57，含并发能力探测旧红新绿用例          |
| `node --import tsx --test packages/services/test/anyAgentService.test.ts packages/services/test/anyagentZcodeAdapter.test.ts packages/services/test/promptAttachmentTransferService.test.ts`                                                                                                                                                                                   | Service／Adapter／附件 76/76                       |
| `node --import tsx --test apps/zcode-cli/packages/bootstrap/test/native-turn-terminal-provenance.test.ts apps/zcode-cli/packages/bootstrap/test/session-mapper.test.ts apps/zcode-cli/packages/bootstrap/test/slash-commands.test.ts apps/zcode-cli/packages/bootstrap/test/stopped-turn-sqlite-resume.test.ts apps/zcode-cli/packages/core/test/stopped-turn-history.test.ts` | 原生映射等 28/28                                   |
| `NODE_OPTIONS=--max-old-space-size=1024 pnpm --dir packages/ui test`                                                                                                                                                                                                                                                                                                           | 实际挂载／DOM 33/33；React `act` 告警仍存在        |
| `pnpm typecheck`；`pnpm --dir apps/zcode-cli/packages/core typecheck`；`pnpm lint`；`pnpm architecture:check -- --changed`；`pnpm build:bootstrap`；`bash docs/decisions/generate-index.sh --check`                                                                                                                                                                            | 均通过；lint 0 error／65 warning，架构 0 violation |

本机完整命令输出保存在 `/tmp/pr25-f47-{contract,runtime,service,native,ui,type,cli-type,lint,arch,build,adr}.log`；桌面启动日志为 `/tmp/pr25-f47-{desktop,m0-desktop}.log`。仓库无适用 `.github` CI；PR 检查列表为空。以上检查不代表正式窗口的全部能力已验收。

## 同一产品候选的原生桌面记录

1. M1 打开时从原侧栏进入旧 Task `task_aafeaad6-2c3f-45a6-a996-d3b10a3a414f`。历史先只读、输入禁用；显式“恢复原会话”后继续使用原产品 Session `session_659c88b4-0d0a-460a-a9db-5f6d33898e7e` 和原生 Session `sess_18b475fb-0313-4a58-888d-f753f46c695f`。这一 Task 的前四轮属于较早 `dda479f` 候选；本次恢复后的新增输入、编辑分别有独立 Input／Execution。[本次附件双库选取字段](attachment-edit-crosscheck.json)证明没有以新 Task 或新 Session 冒充恢复。配置或授权失效时的窗口负例未测。
2. 在原 Composer 一次选择隔离项目内 `pr25-attachment-keep.txt`、`pr25-attachment-remove.txt`。两枚附件芯片进入同一源轮，产品 Input `input_8b89f723-ae18-46aa-9a5a-373b4a0ab553`、原生 user message `msg_mugi9iar_bb2298d0-8484-4cb5-aba7-f5c021d26a81` 都有这两个文件。**该轮模型回答错误地重复上一轮 `TOOL_READ_DDA_OK`**；本次仅证明附件经正式路径到达原生输入，不能称模型读对源轮。
3. 点击原消息“编辑”，草稿回填两枚附件；保留 keep、移除 remove、从原文件选择器加入 `pr25-attachment-add.txt`。新产品 Input `input_fbab06e6-74a8-44b4-934c-bcb0a2b6a8ee` 的 `revisionOf` 指向源 Input／Execution，原生 revision command 为 `revision_a5e9c483-2153-4c42-80ec-459c2f66e6e4`。原生新 user message 只有 keep／add 两个 file parts，真实模型回答分别含 `PR25_ATTACHMENT_KEEP_925`、`PR25_ATTACHMENT_ADD_925`；源产品记录仍保留 keep／remove。[同场景双库字段](attachment-edit-crosscheck.json)。无附件改写、单独保留和更多编辑组合仍缺本候选窗口证据。
4. 原 Composer 输入 `/plugins status` 打开原生插件状态对话框，列出启用的 `node-repl-host@zcode-plugins-official` 0.6.0 和 `browser-use@zcode-plugins-official` 0.5.1；该只读命令前后产品业务 Input 和 Execution 均为 6，没有补造业务轮。[选取字段和 UI 观察](plugins-readonly-crosscheck.json)。`/plugins list` 曾在 `ae019ad` 候选测试；本次不能由 status 推断全部 slash 命令通过。
5. 退出隔离 App，用同一代码提交和 `ANYAGENT_M1_WORKBENCH=0` 重启；原 Root、原 Composer、仅 Provider 的模型选择器、自动化和 Provider 模型设置可达。隔离 OpenCode Go 真实回复 `M0_F47_OK`；原生 Input `queue_01a0d6ff-d21d-7217-9dcf-d32c6950a89c` 为 promoted，同一 Turn `turn_32163bac-7f0e-475c-ba79-c0941517da63` 返回该文本。[M0 原生存储选取字段](m0-native-crosscheck.json)。本次仅覆盖这些 M0 基本路径，不推断其他旧能力无共同退化。
6. 再以 M1 flag 重启，在原侧栏进入同一旧 Task，历史先只读且不自动取得执行资格；显式恢复后从原 Composer 提交 `/compact PR25_F47: Keep the attachment marker context.`。产品 `compact-operation` 经 accepted 到 completed，原生 ACK 与终态均有证据，前后业务 Input／Execution 数仍为 6。随后同一原生 Session 的普通轮 `input_95ea1698-58bc-49c7-a10a-f14a20ff8441` promoted，真实模型答 `COMPACT_F47_OK`。[操作、续聊与双库字段](compact-stop-crosscheck.json)。busy、重复提交及失败结果尚未测。
7. 同一 Task 用原 Composer 开始长真实模型轮 `PR25_F47_STOP`，在尚未完成且“请求中断”可用时点击。正式 UI 短暂提示请求已提交、仍待实际停止证据；最终显示“已停止／已确认停止；没有回答正文”。产品 stop request `stop_cc8eb6a5-9964-4792-a76e-b95c1e8ea4e2` 的送达为 delivered，停止状态 confirmed；原生同 Turn `turn_89544518-bb37-4afc-b0d5-275c87f8d827` 的终态 `turn_complete/cancelled`，原生 user 历史标为 `withdrawn_after_stop`，产品 Input／Execution 都为 stopped。[同场景双库字段](compact-stop-crosscheck.json)。本轮在模型输出正文前停止，不能用它证明完成前回答分片；断线未知对账另属未验。
8. 从原“新建任务”选择 Fake 建 A 连续两轮，经 `⌘N` 和原聚合选择器建独立 Fake B 一轮，再从原侧栏回 A 第三轮；离开至“自动化”后回 A，原消息流仍为 A 的三轮及各轮 Read 工具，B 仍只一轮。产品 SQLite 中 A 三个 Input 共享同一 Task／Participant／Session，B 三身份均独立，四轮均 completed。[正式窗口操作和产品选取字段](fake-a-b-a-crosscheck.json)。本次 Fake 回答完成得很快，未捕获完成前分片或工具卡；此项仍是另一矩阵 D。

此前同代码基线的双附件操作曾因两个并发能力检查相互使旧结果失效，UI 报“更新的 Engine 能力检查”，未形成产品 Input。`f47e6b7` 将同 Adapter、同能力基线及配置版本的并发探测合并到一个 in-flight Promise，并保留新旧快照的版本校验；上述双附件成功路径是修复后的正式 App 复测。独立 reviewer 对完整 PR 和该新增改动补审，未提出剩余可复现的 P1／P2 代码问题；复审不代替桌面验收。

本候选窗口经 CUA 操作与观察，但没有成功保存为仓库截图或录屏。保存截图所需的本地浏览器转送已被 Browser Use 拒绝，未用其他入口绕过；可交付的窗口材料仍缺。上述 JSON 只存同一隔离场景的产品／原生 SQLite 选取字段和 UI 观察摘要，不能冒称录屏。断线后原执行对账、失效授权、审批过期、失败轮重试、队列／compact 异常组合、文件撤销未知状态、全部入口与组合导航，以及最终候选其余矩阵必测仍待完成。因此该候选**尚不具备合并评审条件**，PR、Issue、Milestone #2 保持 open。
