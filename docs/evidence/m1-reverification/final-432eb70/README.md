# PR #25 产品代码 `432eb70` 收口复验

完整被测产品代码提交：`432eb7075024639b1e5e9f0692c20666f85d7fa7`。基线 `origin/main` 为 `644120db21f654edbf40b4008df92294772a08cc`。这是当前产品候选的实际证据；最终分类只在[现有 M0 单参与者收口矩阵](../README.md#m0-单参与者能力收口矩阵)维护。此前候选的证据保留原提交归属。

## 环境、层级与检查

macOS Darwin arm64、Node 24.14.0、pnpm 10.33.2、Electron 41.0.3、固定真实 `zcode-cli` 0.16.9、仓内 ZCode Adapter `m1.2`。隔离数据、Electron 用户目录和隔离项目在 `/tmp/anyagent-opencode-go.aBEA0z`。正式 UI 经 AnyAgent Host / Runtime、ZCode Adapter、既有 ZCode 服务和真实 CLI 调用隔离 App 内已配置的 OpenCode Go (Responses)；本候选使用 `gpt-5.6-luna`、`grok-4.6`，分享续聊沿用 OpenCode Go (Messages) 的 `qwen3.8-flash`。没有把凭据写入截图、日志选取字段或仓库。

分享导入使用仅监听 `127.0.0.1:43127` 的隔离 fixture `pr25-fixture-b9725cc48081448ab41b`；网页元素使用仅监听 `127.0.0.1:43128` 的本地页面。两者证明本机 Host／CLI 路径，不证明生产分享服务。截图、选取的产品 SQLite 字段与原生 SQLite 字段均来自这些隔离运行。原始运行日志仅在本机 `/tmp/pr25-final-432eb70-app.log` 或 launcher 终端；仓库保存可复核的脱敏选取字段。

| 本机检查                                                                                                                                                                                                                                                                                                                                                                       | 实际结果                                            |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------- |
| `pnpm --dir packages/anyagent-engine test`                                                                                                                                                                                                                                                                                                                                     | Contract 9/9                                        |
| `pnpm --dir packages/anyagent-runtime test`                                                                                                                                                                                                                                                                                                                                    | Runtime 53/53                                       |
| `node --import tsx --test packages/services/test/anyAgentService.test.ts packages/services/test/anyagentZcodeAdapter.test.ts packages/services/test/promptAttachmentTransferService.test.ts`                                                                                                                                                                                   | Service / Adapter / attachment 75/75                |
| `node --import tsx --test apps/zcode-cli/packages/bootstrap/test/native-turn-terminal-provenance.test.ts apps/zcode-cli/packages/bootstrap/test/session-mapper.test.ts apps/zcode-cli/packages/bootstrap/test/slash-commands.test.ts apps/zcode-cli/packages/bootstrap/test/stopped-turn-sqlite-resume.test.ts apps/zcode-cli/packages/core/test/stopped-turn-history.test.ts` | 28/28                                               |
| `NODE_OPTIONS=--max-old-space-size=1024 pnpm --dir packages/ui test`                                                                                                                                                                                                                                                                                                           | 实际挂载 / DOM 33/33；React `act` 告警仍存在        |
| `pnpm typecheck`；`pnpm --dir apps/zcode-cli/packages/core typecheck`；`pnpm lint`；`pnpm architecture:check -- --changed`；`pnpm build:bootstrap`；`bash docs/decisions/generate-index.sh --check`；修改文件格式及 `git diff --check`                                                                                                                                         | 均通过；lint 0 error / 65 warning，架构 0 violation |

上述是本机结果，日志 `/tmp/pr25-current-{contract,runtime,service,native-v2,ui,typecheck,lint,architecture,build}.log`。仓库没有适用 `.github` CI；PR 检查列表为空。检查不替代以下真实窗口验收。

## 同一候选的正式 App 与双库对账

| 场景                          | 操作、结果与证据                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ----------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 原 Task、多轮与逐轮模型正例   | 从原“新建任务”和聚合模型选择器创建 ZCode Task；同一产品及原生 Session 首轮 `gpt-5.6-luna` 返回 `R1_432_OK`，第二轮改为同 Provider 的 `grok-4.6` 返回 `R2_GROK_432_OK`；第二轮由键盘 Return 发送。[窗口](two-round-model-switch.png)、[双库身份、Input、模型、Turn](runtime-native-crosscheck.json)。                                                                                                                                                                                                                                                                                                            |
| 真实流式、Stop、send now 队列 | 长源轮在正式消息流输出约 46 行时排 B、A，对 B 点“立即”。UI 先显示中断请求待原生证据，产品 stop-request 最终 `confirmed`；产品 Execution `stopped`；原生源 Turn `cancelled`，原生 user message 标为 `withdrawn_after_stop` / `providerVisibility=hidden`。B 返回且只返回 `QUEUE_B_432_ONLY`，随后 A 返回且只返回 `QUEUE_A_432_ONLY`，没有续写源轮要求的 120 行。[窗口](queue-stop-results.png)、[产品 Input／Stop、原生 input／Turn／回答](runtime-native-crosscheck.json)。这是本候选对早期队列上下文污染的复验。                                                                                               |
| 冷启动恢复                    | 上述 Task 已完成至少两轮后退出隔离 App、重新启动；从原侧栏进入时先显示可读历史、禁用输入及“恢复原会话”，没有自动恢复或重发。显式恢复后原 Task／Participant／产品 Session／原生 Session 保持不变，`grok-4.6` 新一轮返回 `COLD_432_OK`。[恢复前](cold-history-before-restore.png)、[恢复后未派发](cold-restored-ready.png)、[第三轮](cold-third-round-result.png)、[同一双库身份](runtime-native-crosscheck.json)。较早候选停止轮缺少新撤回标记时被安全拒绝，[窗口](legacy-session-restore-blocked.png)；不能将该历史会话计作可恢复。                                                                             |
| 只读 `/goal`                  | 原 Composer 发送无参 `/goal` 后显示“当前 Session 尚未设置目标”；该动作没有创建业务 Input／Execution。之后的双库快照恰有 R1、R2、停止源轮、B、A、冷恢复新轮六个 Input，[选取字段](runtime-native-crosscheck.json)。`/goal` 写侧仍未接通，不能将整条目标命令判 A。                                                                                                                                                                                                                                                                                                                                                |
| 真实 Write 允许、拒绝         | 同一真实 Session 经原 `PermissionDialog` 一次允许 Write，同一 toolCallId 的原生 Write part 仅 1 条，工具输入与原生文件状态均为 `TOOL_ALLOW_432`、回答 `TOOL_DONE_432` 且原文件摘要可见；另一轮选择拒绝，模型确认未写入，隔离文件确实不存在。[允许待答](native-tool-approval-pending.png)、[允许完成](native-tool-approval-result.png)、[拒绝待答](native-tool-deny-pending.png)、[拒绝结果](native-tool-deny-result.png)、[产品审批／执行与文件状态](tool-approval-rewind-crosscheck.json)。过期或迟到答复负例仍未验。                                                                                          |
| 文件撤销                      | 对上述允许 Write 的文件点击原“撤销”：预览显示安全 1、不安全 0；先取消，核对文件仍为 `TOOL_ALLOW_432`；重开预览后执行，文件消失，产品 `file-rewind-operation=applied`，UI 显示“已撤销”且按钮禁用。[预览](native-file-rewind-preview.png)、[执行后](native-file-rewind-result.png)、[产品与文件状态](tool-approval-rewind-crosscheck.json)。未知结果下的防重复执行还缺故障注入证据。                                                                                                                                                                                                                              |
| `/compact` 与续聊             | 原 Composer 发送 `/compact`，产品维护操作 `completed`，没有补造业务 Input／Execution；随后同一原生 Session 的普通轮回答 `COMPACT_CONTINUE_432`。[维护后窗口](native-compact-completed.png)、[续聊](native-compact-continued.png)、[存储](compact-crosscheck.json)。busy／重复提交异常组合未验。                                                                                                                                                                                                                                                                                                                 |
| 网页元素                      | 原内置浏览器打开本地 fixture，点击“选择网页元素加入聊天”再直接点按钮，原 Composer 得到“1 个网页元素”芯片。提交后产品 Input、原生 Input、原生 user message 和真实回答都含 `PR25_WEB_ELEMENT_MARKER_925`；原生请求保存了包含 URL／selector 的展开文本，没有单独 web contextRef。[芯片](web-element-chip.png)、[结果](web-element-result.png)、[两库](web-element-crosscheck.json)。                                                                                                                                                                                                                               |
| 共享上下文                    | 第一次用已在早期候选导入的旧 fixture 代码，Host 返回旧 unknown Task，UI 按身份及状态校验拒绝；这不能代表新导入失败。该失败提示只在本次桌面自动化观察到，未留下可判定的截图；旧 Task 的 `unknown` 状态在产品 SQLite 可复核。换新本机分享代码后，原“添加上下文”创建新的 Task／Participant／产品和原生 Session及授权，真实 CLI 输入带同一 `sharedContextRefs`，模型回答包含新 fixture 的唯一 marker。[新 Task](shared-context-imported.png)、[回答](shared-context-native-result.png)、[双库与 fixture](shared-context-crosscheck.json)。回答另带一句解释，未满足提示的“只输出 marker”，不得称其严格格式遵循通过。 |
| 原生结构化提问                | 在原 Task 输入要求调用 `AskUserQuestion`；真实 CLI 发出 A/B 结构化问题，正式 UI 选 B 后同一 Execution 继续并回答 `QUESTION_432_B`。[待答](native-question-pending.png)、[继续](native-question-answered.png)、[产品请求归属、原生交互 ID 与 Turn](question-crosscheck.json)。过期与迟到答复跨执行负例仍是 D。                                                                                                                                                                                                                                                                                                   |
| 文件 mention                  | 原“添加上下文”插入隔离项目 `m1-attachment.txt` 引用，经真实 Read 工具返回 `ATTACHMENT_MARKER_M1_924`。[草稿](file-mention-draft.png)、[工具结果](file-mention-result.png)、[产品与原生输入](file-mention-crosscheck.json)。本地附件上传和编辑保留／移除／新增没有在本候选重跑。                                                                                                                                                                                                                                                                                                                                 |
| 反馈与分叉                    | 导入 Task 的真实回答做 Like，跨 Task 返回后状态保持；刷新当前资格后改为 Dislike，再撤回，原生最终反馈为空。[窗口](feedback-after-return.png)。从同一回答点“分叉”，新产品 Task／Participant／Session／授权及原生 Session 都与源不同，`forkedFrom` 保留源轮；源轮和子轮的产品提交配置及原生 assistant 均为 `opencode-go-messages/qwen3.8-flash`；子轮经真实 CLI 完成。[子 Task](fork-child-created.png)、[实际子轮](fork-child-result.png)、[来源与身份](fork-crosscheck.json)。子轮模型实际回答 `PR25_OK` 而非提示要求的 `FORK_CHILD_432_OK`，这是本次模型输出偏差，不能拼成“精确回答”证据。                     |
| Fake A→B→A 与导航             | 原入口创建 Fake A 两轮、快捷键 `⌘N` 创建独立 B 一轮，侧栏回 A 发第三轮；再去自动化返回。产品 SQLite 中 A 三轮均属同一 Task／Participant／Session，B 为独立三身份；原消息流有分片和 Read 工具行。[A→B→A](fake-a-b-a.png)、[自动化返回](fake-return-from-automation.png)、[四个 Input 身份](fake-identity-crosscheck.json)。本候选未设置慢分片以截取 Fake 完成前工具状态。                                                                                                                                                                                                                                        |
| 关闭 M1 的 M0 对照            | 在同一代码提交关闭 `ANYAGENT_M1_WORKBENCH` 重启隔离 App，原 Root、原 Composer 和仅 Provider 的模型选择器仍可用；OpenCode Go 的 `gpt-5.6-luna` 返回 `M0_432_OK`。自动化和原模型设置页也可达，设置页只列原 Provider。[原路径](m0-before-send.png)、[结果](m0-result.png)、[Provider 菜单](m0-provider-picker.png)、[自动化](m0-automation.png)、[模型设置](m0-model-settings.png)、[原生 Input / Session](m0-native-crosscheck.json)。预期来自已确认 M0 基线；只证明此次覆盖的旧路径没有共同退化。                                                                                                                |

独立 reviewer subagent 对完整 PR 至本产品提交的代码做了复审：修复了停止轮撤回、冷恢复旧状态、控制命令误判、UI 迟到响应污染等 P1/P2；在 `432eb70` 未找到剩余可复现的 P1/P2 代码阻塞。审查不代替最终桌面验收。独立复核后将逐轮换模型行维持 D：两次成功换模的产品 / 原生字段已在 JSON 对齐，但本候选没有执行“失败配置不改写旧轮”负例。Stop 原生消息的选取字段最初误从 `metadata.inputIntent` 提取撤回状态；已改为直接读取同一消息的 `metadata`，不涉及产品行为变更。两次冷启动仍由 Runtime 构造逻辑为 active Session 写入 `stream-ended-unknown` 历史诊断，即使所有 Input／Execution 已终态；双库和代码核对表明它们是“重启待重连”的误分类，不是实际断线事件。该诊断问题尚未修正，也不能充当断线恢复证据。

## 未完成的既定范围

完整矩阵仍有 D：固定 CLI 其余可用 slash 命令和 `/goal` 写侧、真实断线／恢复对账与有副作用不重发、冷恢复配置／授权变化与终态、过期审批／迟到答复、失败轮安全重试、附件编辑全部组合、队列与 compact 异常组合、文件撤销未知结果、所有新建入口与导航焦点组合、权限／推理档位边界等。旧候选截图不能替代这些最终候选必测证据；没有已确认的能力豁免。当前不满足“技术阻塞项清零”或合并评审条件；PR、相关 Issue 和 Milestone #2 继续保持 open。
