# `b0fc0e9` 最终产品候选的本机复验

完整被测产品提交：`b0fc0e97ccac4127a20d6eba11a30fddfbcf3a34`。基线 `origin/main`：`644120db21f654edbf40b4008df92294772a08cc`。本目录只记录该产品提交上的实测；[唯一收口矩阵](../README.md#m0-单参与者能力收口矩阵)保留尚未完成项。证据整理不修改产品代码、依赖、构建或装配。

## 环境、运行与检查

macOS Darwin arm64，Node 24.14.0、pnpm 10.33.2、Electron 41.0.3、固定真实 `zcode-cli` 0.16.9、仓内 ZCode Adapter `m1.2`。隔离 home、项目和 Electron 数据位于 `/tmp/anyagent-opencode-go.aBEA0z`；App 复用其中已配置的 OpenCode Go (Responses) `gpt-5.6-luna` 与 `grok-4.6`，没有读取或记录凭据。M1 用该隔离启动器运行仓库 `pnpm dev:desktop:prod`，设 `ANYAGENT_M1_WORKBENCH=1`；M0 在同一代码提交设为 `0`。分享导入测试端口指向本机不可达的 `127.0.0.1:9`，因此本页不声称真实分享导入通过。原始 App 日志仅留在隔离目录 `app-b0fc0e9-m1-second.log`、`app-b0fc0e9-m0.log`，不提交包含环境细节的完整日志。

本机命令与结果：

| 命令                                                                                                                                                                                                                                  | 结果                                             |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ |
| `pnpm --dir packages/anyagent-engine test`                                                                                                                                                                                            | Contract 9/9                                     |
| `pnpm --dir packages/anyagent-runtime test`                                                                                                                                                                                           | Runtime 52/52                                    |
| `node --import tsx --test packages/services/test/anyAgentService.test.ts packages/services/test/anyagentZcodeAdapter.test.ts packages/services/test/promptAttachmentTransferService.test.ts`                                          | Service / Adapter / attachment 73/73             |
| `node --import tsx --test apps/zcode-cli/packages/bootstrap/test/session-mapper.test.ts apps/zcode-cli/packages/bootstrap/test/native-turn-terminal-provenance.test.ts apps/zcode-cli/packages/bootstrap/test/slash-commands.test.ts` | 原生映射、terminal、slash 合计 4/4               |
| `NODE_OPTIONS=--max-old-space-size=1024 pnpm --dir packages/ui test`                                                                                                                                                                  | 实际挂载及 DOM 29/29；React `act` 告警仍存在     |
| `pnpm typecheck`；`pnpm lint`；`pnpm architecture:check -- --changed`                                                                                                                                                                 | 通过；lint 0 error、65 warning；架构 0 violation |
| `pnpm build:bootstrap`；`bash docs/decisions/generate-index.sh --check`；修改文件格式与 `git diff --check`                                                                                                                            | 通过                                             |

原始检查日志为本机 `/tmp/pr25-b0-{contract,runtime,service,native,ui,typecheck,lint,arch,build,adr,format}.log`。仓库没有适用的 GitHub CI 工作流，PR 检查列表为空；这些是本机结果。

## 同一候选的桌面与存储对账

下列窗口操作全部从正式 Root、原侧栏和原 Composer 发起，M1 主路径为 UI → Host / Runtime → ZCode Adapter → 既有服务 → 真实 CLI → 已配置模型。SQLite 查询只读，用来核对 UI 场景对应的产品和原生身份；[选取字段](runtime-native-crosscheck.json)、[中断](stop-crosscheck.json)、[审批与 compact](approval-compact-crosscheck.json)、[撤销](file-rewind-crosscheck.json)均指向同一隔离数据。Fake 用于另列场景，不充当真实 CLI。

| 场景、操作和实际结果                                                                                                                                                                                                                                                                                                                                                                     | 截图或记录                                                                                                                                                                                              |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 退出并重启 App 后从原侧栏进入已有的一轮 M1 Task：历史可读时原生状态仍未知，不能直接发送；显式点“恢复原会话”后用原 Task、Participant、产品 Session、原生 Session 继续下一轮。恢复前的队列编辑草稿仍在；切到同 Provider `grok-4.6`、`xhigh` 后模型返回 `B0_RECOVERED_R2`。被取消的旧队列 Input 没有进入原生 `session_input`。这一场景本身不满足“两轮后重启”的严格条件，下一行另行补测。    | [历史与未知状态](cold-history-unknown-queue-draft.png)、[恢复草稿](cold-restored-draft-ready.png)、[续聊](cold-restored-model-switch-r2.png)、[存储](runtime-native-crosscheck.json)                    |
| 更严格的冷启动复测：同一真实 CLI Task 已完成拒绝、允许及 compact 后续聊共三轮，退出 App，重新启动；从原侧栏打开时三轮历史可读，但输入禁用且原生 Session 为未知。显式恢复后第四轮返回 Markdown 标题、TypeScript 代码块和 `COLD_R4_B0`。产品 Task / Participant / Session 及原生 Session 保持原 ID；原生 `session_input` 顺序为 0、1、2、3，没有新建伪 Session，也没有重发已完成文件操作。 | [重启后历史与未知](cold-two-round-history.png)、[显式恢复](cold-two-round-restored.png)、[第四轮 Markdown](cold-two-round-resumed-markdown.png)、[双层存储](cold-two-round-crosscheck.json)             |
| 独立真实 CLI Task 正在 Read 后流式生成 60 条建议时，第二输入进入队列；原 UI 编辑将该 Input 标为 cancelled，并还原文字到 Composer。离开到自动化再返回后草稿未丢；改成新文本后只发出新一轮，原生序号只有源轮与编辑后轮。                                                                                                                                                                   | [运行中编辑](real-cli-queue-edit-during-source.png)、[导航返回](real-cli-queue-draft-after-navigation.png)、[完成](real-cli-queue-edited-complete.png)、[存储](runtime-native-crosscheck.json)          |
| 从正式 UI 请求中断真实 CLI 的 100 条长回答：UI 先显示等待确认；StopRequest 送达后才以原生证据将 Input / Execution 标为 stopped。请求、送达、停止是独立记录。                                                                                                                                                                                                                             | [请求前](stop-before-request.png)、[等待](stop-request-pending.png)、[确认](stop-confirmed.png)、[存储](stop-crosscheck.json)                                                                           |
| 同一真实 CLI Task 首轮 Write 审批点“拒绝”，`m1-b0-deny.txt` 没有创建；下一轮 Write 点“允许”，`m1-b0-allow.txt` 曾包含 `ALLOW_ONCE_B0`，UI 显示工具和一项文件摘要。拒绝与允许对应不同 Approval 和 Execution。                                                                                                                                                                             | [拒绝待答](approval-deny-pending.png)、[拒绝结果](approval-denied.png)、[允许待答](approval-allow-pending.png)、[允许结果](approval-allowed-file-summary.png)、[存储](approval-compact-crosscheck.json) |
| 对允许轮的文件摘要打开撤销预览，安全 1 / 不安全 0；关闭预览后文件内容仍为 `ALLOW_ONCE_B0`。重开后点撤销，文件实际消失、产品 `file-rewind-operation` 为 applied，UI 显示“已撤销”。                                                                                                                                                                                                        | [预览](file-rewind-preview.png)、[执行后](file-rewind-applied.png)、[存储](file-rewind-crosscheck.json)                                                                                                 |
| 同一真实 CLI Session 经键盘提交 `/compact PR25_B0_KEEP_ALLOW_RESULT`，compact 从 accepted 到 completed；未造业务 Input / Execution。随后普通输入完成并返回 `COMPACT_RESUME_B0`。busy 与重复提交路径未验。                                                                                                                                                                                | [提交](compact-after-submit.png)、[完成](compact-completed.png)、[续聊](compact-resumed-round.png)、[存储](approval-compact-crosscheck.json)                                                            |
| `/locale status` 在原 Composer 给出当前语言提示，不创建产品 Input。                                                                                                                                                                                                                                                                                                                      | [截图](locale-status-no-input.png)、[存储](runtime-native-crosscheck.json)                                                                                                                              |

产品身份示例：首个冷恢复 Task `task_6b939111-1648-428c-802c-dd1ab0afe7e9`，产品 Session `session_bc9dfaf2-fac4-4e50-a694-309a5c7f3a76`，原生 Session `sess_b7159530-16fb-4ec5-8ff1-fd6cef59324a`；满足退出前至少两轮的严格复测 Task `task_ba6c9166-e938-45bf-a7ed-fef8a41e2ac0`、原生 Session `sess_418b065d-7596-481c-982e-273f7ab1afc7`；中断 Task `task_f46d7224-cb85-44b7-bdab-5436da143af4`。完整 ID 和每轮配置见对应 JSON。没有将同 Task 的合法恢复当作跨 Task 复用；跨 Task 防护由 Runtime/Service 测试覆盖，桌面组合仍在矩阵 D 项。

## Fake 与 M0 对照

Fake Engine 在当前候选上走原侧栏：A 三轮、B 独立一轮，A→B→A 后又离开到自动化并返回；A 的三轮仍归同一产品 Task / Session，B 的身份不同。[A→B→A](fake-a-b-a.png)、[再次返回](fake-return-from-automation.png)、[产品选取字段](runtime-native-crosscheck.json)。Fake 分片/工具及故障测试属于模拟层，不代表模型或真实 CLI。

关闭 M1 开关后，同一候选 URL 无 `anyAgentServiceEnabled`，原选择器只列 Provider；原 Composer 使用 OpenCode Go `gpt-5.6-luna` 的真实 M0 路径返回 `M0_B0_NATIVE_OK`，原模型设置页可达。[选择器](m0-provider-selector.png)、[回应](m0-native-result.png)、[设置](m0-model-settings.png)。ZC-04 与 M0 原实现给第一层交互基线；这次开关关闭的实际操作给第二层回归证据，只覆盖所列基本路径。

## 未闭合项与审阅

同一最终候选尚未完成隔离 CLI 连接断开及恢复对账、真实网页和分享引用、全部可用 slash 命令、队列 send now / 排序 / 未知恢复、附件编辑全部组合、结构化提问和过期审批的最终窗口证据、失败轮重试、文件撤销未知结果、所有新建 Task 入口与组合导航、当前能力与失效授权变化等。较早候选的单项结果留在上级台账，不能替代这些必测项。没有维护者确认的能力豁免；这些仍按 D 处理。

独立代码复审发现队列编辑草稿在持久化写入失败、跨 Task 返回以及 A 状态清理后 B 异步恢复时有丢失或错误合并风险；`6f84500`、`8aeed9f`、`3c6bd41`、`eb2e832`、`b0fc0e9` 逐步修复，最终复审未再指出该范围代码阻塞。审阅是代码层证据，不替代桌面验收，也不授权合并。
